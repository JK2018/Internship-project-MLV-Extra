<span class="rating">
    {% for i in 1..5 %}
        {% if i <= rating %}
        <i class="fas fa-star"></i>
        {% else %}
        <i class="far fa-star"></i>
        {% endif %}
    {% endfor %}
</span>
{# use with include and pass the correct variable for rating. 
example: {% include 'shared/rating.html.twig' with {'rating': coment.rating} %} #}