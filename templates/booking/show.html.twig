{% extends 'base.html.twig' %}

{% block title %} Confirmation n°{{booking.id}} {% endblock %}
{% block body %}

    <div class="container">
        <h1>Votre confirmation de mission (n°{{ booking.id }})</h1>  

{% if app.request.query.get('withAlert') %}
    <div class="alert alert-success">
        <h4 class="alert-heading">Bravo !</h4>   
        <p>
            Votre mission pour <strong> <a href="{{ path('ads_show', {'slug': booking.ad.author.slug}) }}">{{ booking.ad.title }}</a> </strong> a bien été pris en compte !
        </p> 
    </div>
{% endif %}

        <div class="row">
            <div class="col">
                <div class="alert alert-light">
                    <h2>Détails</h2>
                    <dl class="row">
                        <dt class="col-md-4">Numéro de confirmation</dt>
                        <dd class="col-md-8">{{ booking.id }}</dd>
                        <dt class="col-md-4">Premier jour de travail</dt>
                        <dd class="col-md-8">{{ booking.startDate | date('d/m/Y') }}</dd>
                        <dt class="col-md-4">Dernier jour de travail</dt>
                        <dd class="col-md-8">{{ booking.endDate | date('d/m/Y') }}</dd>
                        <dt class="col-md-4">Nombre de jours travaillés</dt>
                        <dd class="col-md-8">{{ booking.duration }}</dd>
                        <dt class="col-md-4">Total heures travaillées</dt>
                        <dd class="col-md-8">{{ booking.amount }}</dd>
                        <dt class="col-md-4">Commentaire</dt>
                        <dd class="col-md-8">{{ booking.comment | default('aucun commentaire') }}</dd>
                    </dl>
                    <hr>
                    <h2 class="alert-heading"> Votre responsable </h2>
                    <div class="row mb-3">
                        <div class="col-md-3">
                            <img src="{{ booking.ad.author.picture }}" class="avatar avatar-medium" alt="avatar de {{booking.ad.author.firstName}} {{booking.ad.author.lastName}}">
                        </div>
                        <div class="col-9">
                            <h4>
                                <a href="{{ path('user_show', {'slug': booking.ad.author.slug}) }}">{{booking.ad.author.firstName}} {{booking.ad.author.lastName}}</a>
                            </h4>
                            <span class="badge badge-primary">{{ booking.ad.author.ads | length }} annonces </span>
                        </div>
                    </div>
                    <p>Vous rencontrez un problème, avez un empèchement, vous êtes trompé sur les dates, ou simplement souhaitez annuler ? Merci de me prévenir rapidement au numéro suivant ! tel : {{ booking.ad.author.tel }}.</p>
                </div>
                {# ---- AVIS ---- #}
                <div class="alert alert-light">
                    <h2 class="alert-heading">Votre avis compte! </h2>
                    {#  not over yet / mission over but not commented / mission over and commented  #}
                    {% if date() > date(booking.endDate) %}
                        {% set comment = booking.ad.getCommentFromAuthor(app.user) %}
                        {% if comment is not null %}
                            <blockquote>
                                {{comment.content}}
                            </blockquote>

                            <strong>Note :</strong> {% include 'shared/rating.html.twig' with {'rating': comment.rating} %}
                        {% else %}
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <button type="submit" class="btn btn-success">Confirmer!</button>
                            {{ form_end(form) }}
                        {% endif %}
                    {% else %}
                        <p>Ici, vous pourrez donnez votre avis sur cette mission une fois que vous l'aurez terminée</p>
                        <p>Revenez plus tard et partagez-nous votre expérience!</p>
                    {% endif %}
                </div>
            </div>
            {# ---- Mission recap ----#}
            <div class="col">
                <div class="alert alert-light">
                    <h2 class="alert-heading"> Votre mission </h2>
                    <h4>
                        <a href="{{ path('ads_show', {'slug': booking.ad.author.slug}) }}">{{ booking.ad.title }}</a>
                    </h4>
                    <img src="{{booking.ad.coverImage}}" alt="image de {{ booking.ad.title }}" class="img-fluid">
                    {{booking.ad.content | raw}}
                    <a href="{{ path('ads_show', {'slug': booking.ad.slug}) }}" class="btn btn-primary">Plus d'informations</a>
                </div>
            </div>
        </div>  
    </div>

{% endblock %} 